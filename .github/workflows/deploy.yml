name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2   

      # - name: Test SSH connection
      #   env:
      #     SSH_KEY: ${{ secrets.SSH_KEY }}   
      #   run: |
      #       echo "${SSH_KEY}" > ssh_key
      #       chmod 600 ssh_key
      #       ssh -o StrictHostKeyChecking=no -i ssh_key root@37.27.95.239 'echo SSH connection successful'
        
      - name: Run deployment script on server
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}
        run: |
          echo "${SSH_KEY}" > ssh_key
          chmod 600 ssh_key
          ssh -o StrictHostKeyChecking=no -i ssh_key root@37.27.95.239 'cd /var/www/imchatty/imchatty && git pull origin main && bash deploy.sh'
    
